# Cloudflare Workers/Pages configuration for the DCIM Dashboard
# This deploys the built React app as static assets

name = "dcim-dashboard"
compatibility_date = "2024-01-01"

# Deploy the Vite build output as static assets
[assets]
directory = "./dist"
