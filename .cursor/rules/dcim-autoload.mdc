---
description: Current DCIM DART v0.9 worktree context; load for every conversation
alwaysApply: true
---

# DCIM DART v0.9 context

Read `AGENTS.md`, `STATUS.md`, and every file under `specs/dart-v0.9/` before acting.

Current capability boundary:

- zero real corroborated rows;
- zero New York official-record adapters connected to scoring;
- NYISO exists but is unwired;
- no DART parser, adapter, packet, API route, or UI path exists yet;
- no current detection or measured lead-time claim is valid.

The active objective is one real DART-derived packet that collapses to one canonical origin and renders `WITHHELD_ONE_ORIGIN` / `insufficient_sources` / `suppress`, with no score and no corroboration.

During T01, operate read-only and stop after the repository/code-map report. Do not run the application, build, tests, dependency installation, network retrieval, Git mutation, push, deployment, publication, or outreach unless the active task explicitly authorizes the exact act.

Do not treat legacy root documentation as current product authority. Do not introduce a second writer, a new framework, a state library, or a dependency without explicit task authority.
