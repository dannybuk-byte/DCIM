# Principal rulings of 2026-08-05 — repository implementation record

Context: the 2026-08-03/05 reconciliation rounds (Codex terminal retrieval, Cursor
workflow delta, Claude repository audit + supplemental delta, two ChatGPT reviews;
canonical record outside the repo at
`~/Library/Application Support/DCIM-Chronolocal-Retrieval/`).

| # | Ruling | Status | Implementation |
|---|---|---|---|
| 1 | Retire the cross-wired corpus row `goldman_sachs_www_tier_i` (Morgan Stanley excerpt/candidate under Goldman identity; sed-assembled from template ~2026-05-15) | **DONE** | `www_pipeline_out/validated_cases.json` → `validated_cases.json.RETIRED_2026-08-05` (+ retirement note, both git-ignored). Engine fails closed in live mode per R-F3 — the honest state: zero internally consistent real corpus rows. Re-author via `emit_case_cards.py` from a company-matched candidate. |
| 2 | Floor counts independent origins, not raw admitted rows (F32 design, previously scratch-only) | **DONE — FAIL CLOSED** | `admissionContract.js` admits only eligible rows with canonical `origin_id` to `admission.counted`; unresolved and standing non-counting rows remain support only. `scoringEngine.js` independently validates and deduplicates canonical `origin_id` values; row/source IDs, URLs, and document IDs are never substituted for official-origin identity. Score-row counts, periods, types, confidence, quality, risk, and numeric fields derive only from `admission.counted`; support remains available through raw evidence and warnings. `emit_case_cards.py` stamps `origin_id` (`sec_edgar:<cik>`; one issuer = one origin). Acceptance: `server/originFloor.test.js` (T1–T6). Implemented by the third and fourth local ruling commits. |
| 3 | Demote `epoch_ai_data_center` to non-counting confirmation pending a per-family admission dossier | **DONE** | `epochConfirm.js` merge stamps `counts_toward_floor: false`; `admissionContract.js` `NON_COUNTING_SOURCE_TYPES` keeps the type in support and `scoringEngine.js` excludes support from score metadata (belt-and-braces). Epoch rows remain in raw evidence and still drive lead-time. Tests re-pinned: `epochSourceType.test.js` §2.1, `epochConfirm.test.js` §6.2. |
| 4 | Pivot dating: 2026-07-09 governance date; 2026-06-25 documentary origin (`spike_evidence_units/generated/inventory/R1.md`) | RATIFIED (record-only) | — |
| 5 | Engine UI stays dark until rulings 1–2 shipped and one NY official-record adapter lands; then unhide as the Candidate Evidence Console | STANDING | `SHOW_DISCLOSURE_MISMATCH` remains `false`. |
| 6 | Display honesty: mode-gate the demo banner; withhold stat-tile figures until console-live; label DeepDive as simulated | **DONE** | Commit `remediate(display-honesty)` on this branch: `App.tsx`, `OmniscientCommandInterface.tsx` (`statsReady`), `DeepDiveView.tsx` (SIMULATED chrome + disclosure banner). |
| 7 | Round-2 counterparty carrier: send only after rulings 1–3 are implemented, on explicit go | PENDING principal go | Draft (UNSENT) in the 2026-08-05 final internal package. |
| 8 | Epoch ingest execution record: accept artifact + git evidence; close | CLOSED | — |

Test hygiene fixed alongside: `index.rf2.test.js` no longer depends on the operator's
git-ignored local corpus artifact — production-mode assembly now boots against the
committed fixture `server/testFixtures/rf2_corpus.json` via `SIGNALS_SEED_PATH`.

Suite after the support/count correction: 249/249 passing (33 files).
